<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.deepspc.filtergate.modular.examinegate.mapper.ExamineGateMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.deepspc.filtergate.modular.examinegate.entity.ExamineGate">
        <id column="gate_id" property="gateId"/>
        <result column="customer_id" property="customerId"/>
        <result column="customer_name" property="customerName"/>
        <result column="gate_name" property="gateName"/>
        <result column="gate_code" property="gateCode"/>
        <result column="set_address" property="setAddress"/>
        <result column="longitude" property="longitude"/>
        <result column="latitude" property="latitude"/>
        <result column="set_date" property="setDate"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="base_column">
        gate_id "gateId", customer_id "customerId", customer_name "customerName", gate_name "gateName", gate_code "gateCode",
        set_address "setAddress", longitude "longitude", latitude "latitude", set_date "setDate"
    </sql>

    <select id="getExamineGates" resultType="map">
        select
        <include refid="base_column"/>
        from t_gate
        <if test="customerName != null and customerName != ''">
            where customer_name like CONCAT('%',#{customerName},'%')
        </if>
    </select>

</mapper>
